# Tasks: add-cybertopology-app

Phased to match the priority order in `docs/COMPETITOR_IDEAS.md` §10. Each phase ends in a runnable vertical slice.

## 1. Foundation — project + engine bridge

- [x] 1.1 Create Xcode project (SwiftUI, iPadOS target; working title CyberTopology), repo scaffolding, CI (build + `openspec validate --all --strict`)
- [x] 1.1a Test infrastructure: XCTest + XCUITest targets, simulator test job per PR, >90% coverage gate per layer (xccov/llvm-cov), `tests/traceability.yaml` scenario→test map with CI check for unmapped scenarios (spec: quality-assurance)
- [x] 1.1b Stroke-fixture recorder/replayer for gesture integration tests; golden-file harness (Weave/tangents/bakes) (spec: quality-assurance)
- [x] 1.2 Integrate CyberRemesherAndUV: submodule/xcframework build, `CyberKit` Swift package façade, smoke test calling an engine mesh op from Swift
- [x] 1.3 Document model: bundle format, `UIDocument` integration, Files-app-visible folder, autosave + crash recovery, named versions (spec: document-model)
- [x] 1.4 Undo journal on persistent element IDs; two/three-finger tap undo/redo (spec: document-model)
- [x] 1.5 OBJ import (vertex colors) as Target and as EditMesh; OBJ+MTL export — minimal I/O for dogfooding (spec: scene-pipeline)

## 2. Viewport — Metal renderer

- [x] 2.1 Metal viewport: MTKView/CAMetalLayer host, camera (orbit/pinch/reframe, adjustable speed, scale-adaptive clip planes, camera rescue) (spec: viewport-rendering)
- [x] 2.1a Engine C API render-data access (prereq for 2.2–2.4, design D2): deterministic triangulated index buffer, per-vertex normals/colors + has-colors query, zero-copy pointer views with documented lifetime (Engine/patches/0002); CyberKit `triangleIndices`/`normals`/`colors`/`withRenderBuffers` facade (spec: viewport-rendering)
- [x] 2.2 High-poly Target pipeline, fallback scope: indexed-vertex path (smooth shading + vertex colors), capability-gated render-path seam for the meshlet pipeline, pooled no-per-frame-allocation geometry buffers, device-only frame-time perf harness (spec: viewport-rendering) — meshlet/LOD path and the 5M-tri acceptance are 2.2a
- [ ] 2.2a Meshlet/LOD render path on mesh-shader hardware (A14/M1+) behind the 2.2 `TargetRenderPath` seam; 5M-tri @60fps device acceptance run (traceability scenario "Multi-million-triangle target" stays pending until this lands) (spec: viewport-rendering)
- [x] 2.3 EditMesh overlay pipeline: animated wireframe, verts/pins/tags, opacity, occlusion threshold, x-ray mode (spec: viewport-rendering) — pins/loop-tags render styles land with their data model in 4.3
- [x] 2.4 Ghost-geometry render style + zero-copy engine buffer sharing (spec: viewport-rendering) — pooled-copy is the active sharing path until the engine allocates page-aligned VM-backed render caches (upstream issue); ghost accept flow lands with Weave in 5.4
- [x] 2.5 120 Hz ProMotion pacing, resolution scale option, MetalFX (spec: viewport-rendering) — render-on-demand pacing (idle = zero draws) with the 120 Hz range on ProMotion; MetalFX spatial upscaling is capability-gated (simulator exercises the plain scaled fallback; the upscale itself is device-gate territory, task 9.6)

## 3. Input & gesture layer

- [x] 3.1 Touch/Pencil arbitration state machine (pen vs 1/2/3+ fingers, palm rejection, hold-chords); finger fallback for all pen gestures (spec: pencil-interaction) — pure `InputArbiter` + UIKit glue under the existing camera/undo recognizers, minimal spring-loaded verb toolbar (full toolbar is 3.8), draw-mode finger fallback, stroke capture → `StrokeConsumer` (debug consumer until the 3.2 recognizer), left-handed mirror stub; hardware-only Pencil behaviors (hover, Pencil Pro) remain 3.6/3.7
- [x] 3.2 Engine gesture recognizer integration: stroke sampling → shape classifier → mesh-context resolver; interpretation records; dev debug HUD (spec: pencil-interaction) — engine-side two-stage recognizer (patches 0004 spatial queries + 0005 `stroke_interpreter.hpp` + `cyber_stroke_interpret` capi), `StrokeInterpreter`/`StrokeInterpretation` CyberKit facade, `StrokeRecognizerConsumer` bridging the 1.1b fixture format, committed programmatic stroke corpus + golden-filed interpretation records, arbiter→recognizer app wiring with logged interpretations, DEBUG stroke HUD (polyline + record, settings toggle); no mesh mutation yet (applying interpretations is 3.3/3.4, chip UI is 3.5)
- [x] 3.3 Five verbs wired in RT: Pencil/Relax/Move (geodesic falloff)/Tweak/Erase with continuous Target snapping (specs: pencil-interaction, retopology-tools) — engine patch 0006 (mutating `cyber_retopo_*` capi ops wrapping the engine's create-face/tweak/geodesic-move/relax/erase with render-cache invalidation per the 0002 LIFETIME contract, plus quad-corner estimates on interpretation records); CyberKit typed verb ops + `MeshEditTransaction` → journaled `DocumentCommand.meshEdit` with byte-exact before/after payloads (mesh-level undo; engine-inverse changesets remain an upstream TODO); app `MeshEditController` wired capture→recognizer→verbs (Pencil createQuad on empty surface executes; Relax/Move/Tweak/Erase are live brush sessions journaled once per stroke, cancel discards); verbs currently require a Target to anchor to (surface-free editing is later scope); golden-filed canonical quad-create; UI quad-draw test injects the committed square fixture (XCUITest cannot synthesize polyline touches — raw-touch path covered by the straight-line draw-mode UI test + controller suite)
- [x] 3.4 Full gesture grammar: quad draw, grid stroke, loop insert/tag, scribble dissolve, X delete, merge line, double-tap tweak/slide, edge rotate, visibility lasso/lines (spec: pencil-interaction) — engine patches 0009 (quad-ring/edge-loop walks + `cyber_retopo_insert_loop`: the FULL-ring loop insert — the pre-existing `insertLoop` split exactly one quad), 0010 (dissolve/merge/rotate capi ops incl. quad-pair rotation), 0011 (recognizer grammar v2: grid-stroke shape with lattice estimate, whole-ring/whole-loop elements for insert-vs-tag disambiguation, X-over-region faces, lasso start-in-empty-space rule, visibility line restricted to empty space), 0012 (per-handle hidden-face/tagged-edge render filters), 0013 (welded `cyber_retopo_create_grid`); `MeshAnnotations` document state (loop tags + hidden faces) on the manifest object journaled via new `DocumentCommand.annotationEdit`; `MeshEditController` executes the full grammar (topology ops journaled as byte-exact `meshEdit`, annotations as `annotationEdit`, one entry per stroke incl. the whole grid block; visibility direction from the raw stroke; double-tap on a VERTEX activates Tweak); minimal green tagged-edge overlay pass (full styles 4.3); committed provenance-pinned stroke fixture + golden per grammar entry replayed through the real engine recognizer, engine-op goldens, and end-to-end capture→recognizer→controller tests with undo coverage. Split out honestly: 3.4a
- [ ] 3.4a Grammar remainders needing new engine ops/models: double-tap on an EDGE → slide the loop (engine loop-slide op does not exist yet), multi-row one-stroke grid blocks (recognizer currently emits 1×N lattices), grid strokes welding onto EXISTING topology (auto-merge boundary vertices), and lasso-hide of the TARGET mesh (annotation model covers the EditMesh only; Target visibility belongs with the scene outliner scope)
- [x] 3.5 Post-stroke interpretation chip with one-tap alternatives (spec: pencil-interaction) — transient chip after every completed Pencil stroke (recognized, inert, and unrecognized alike) fed by the 3.2 interpretation records: pure `InterpretationChipState` machine (show / replace / dismiss / next-stroke-dismiss / generation-tokened auto-dismiss — never blocks the next stroke) published via `ViewportInputModel`, one-tap alternatives for the ranked candidates whose replacement is rebuildable from the byte-exact pre-stroke snapshot (element ops on a scratch mesh, annotation transforms, stroke-time-unprojected quad corners); choosing one REPLACES the applied result atomically through the journal — new `UndoJournal.replaceCurrent` + `TopoDocument.performReplacingLast(with:expecting:)` revert-apply-swap the node IN PLACE, so exactly one journal entry stands per stroke, a single undo steps over it, and stale chips (undo/commit landed since) are rejected untouched; covered by the spec-direction tagLoop→insertLoop swap through the REAL engine recognizer (corpus-wobble stroke), the seeded-strip model/UI path with swap-back, journal one-entry invariants at all three layers, and the finger draw-mode UI test (stroke → chip → alternative tap → mesh + undo-gesture journal assert). Non-journaling candidates (visibility direction, tweak verb switch) and the first-stroke `addObject` chip stay informational (no swap) — honest scope, alternatives there are not "applied results" to replace
- [x] 3.6 Hover previews (ghost quad, loop highlight, snap-target highlight) (spec: pencil-interaction) — `UIHoverGestureRecognizer` on the viewport (Pencil hover on hover-capable hardware, pointer devices elsewhere) feeding a pure `HoverPreviewState` machine (priority vertex > interior-edge loop > empty-surface ghost, dedupe, cleared on hover end AND the instant any stroke begins) whose queries run engine-side and read-only: empty surface → ghost quad hint at the snap position (nominal screen square landed on the Target like drawn corners, rendered through a second `GhostRenderPath` so the hint never fights the Weave-proposal/DEBUG feed), interior edge → the loop a double-tap would slide via the REAL `cyber_mesh_edge_loop` walk (boundary edges honestly show nothing — slide needs the 3.4a loop-slide op), near-merge vertex → snap-target highlight; loop/snap render as a warm-yellow overlay pass (standalone world-space buffer, allocated only on preview change). State machine unit-tested via injected hover events/query answers; the full query path (coordinator entry → camera ray → engine raycast/picks/loop walk → Metal pipelines, mesh asserted byte-identical) is integration-tested end to end; actual Pencil hover EVENT DELIVERY is hardware-only — explicit device-only XCTSkip (`HoverHardwareTests`) counted against the 9.6 device plan; `-UITestAutoHoverLoop/Ghost` screenshot probes drive the same controller on simulator
- [x] 3.7 Haptics on snap/merge (Core Haptics + Pencil Pro); squeeze radial gallery; barrel-roll rotate (spec: pencil-interaction) — merge-snap detection wired into the Tweak/Move sessions (engine patch 0014 `cyber_mesh_nearest_vertex_excluding`: the dragged vertex would always win the unfiltered nearest query): the snap target pre-highlights through the real overlay highlight pass BEFORE anything commits, and at stroke end Tweak MERGES the dragged vertex into the target / Move welds the seed's position onto it (`tweak.mergeSnap`/`move.vertexSnap`) inside the same transaction — still exactly ONE journal entry per stroke; haptic ticks map through the pure `SnapFeedbackState` (snap-engaged on entering range, commit tick only when the merge actually journaled) against the injected `SnapHapticsPlaying` seam, with the production `SnapHapticsEngine` capability-gated everywhere (`UICanvasFeedbackGenerator` for Pencil-Pro routing + Core Haptics only where `supportsHaptics`; simulator = graceful no-op) and user-disableable via the settings toggle (ticks only — highlight and merge behavior unaffected); Pencil Pro squeeze (`UIPencilInteraction`) opens the minimal radial quick-verb palette at the pen tip honoring the SYSTEM squeeze preference (contextual-palette → ring, switch-eraser → Erase verb, ignore honored; full Action Gallery is 3.8), dismissing on selection/second squeeze/stroke begin; traceability maps the simulator-testable half of "Snap feedback" (pre-highlight + journaled merge + injected-seam tick mapping), while squeeze/barrel-roll DELIVERY and haptic ACTUATION are explicit device-only XCTSkips (`PencilProHardwareTests`, device plan 9.6). Split out honestly: 3.7a
- [ ] 3.7a Barrel-roll rotate-placed-element consumer: the hook is plumbed (hover recognizer `rollAngle` → `ViewportInputModel.barrelRollAngle`/`onBarrelRollChanged`, deduped), but NO task-3.4 op has a free orientation to consume it — quads/grids take their orientation from the drawn stroke and the hover ghost hint is a fixed screen square — so the first real consumer is the 4.2 placement tools (Patch Clone / Draw Strip / Transform Vertices; UV-island rotation follows in phase 6). Also deferred with it: the spec's snap micro-animation polish (the highlight is currently static; animation styles land with 4.3's overlay render styles)
- [x] 3.8 Customizable toolbar + Action Gallery with per-action demo videos; persistence (spec: pencil-interaction) — bounded 6-slot toolbar (`ToolbarConfiguration`, slot scheme documented on the type: one action per slot, no duplicates — assigning elsewhere MOVES; default = the five verbs + one empty slot) replacing the 3.1 minimal verb bar while HOSTING its spring-loaded verb hold-chords unchanged (same `VerbButton`/arbiter path and `verb-*` identifiers); Action Gallery sheet listing every action (5 verbs + the 11 task-3.4 grammar entries, completeness unit-gated) with help panel (title, gesture line, usage notes) and the demo-media slot as a looping SF-symbol frame animation HONESTLY labeled a placeholder — recording real demo videos is 9.1 tutorial content, the entry/view swap in the player without touching callers; customization via drag (gallery tile → slot assign/replace, slot → slot move, slot → tile-area remove, `ToolbarDragPayload` policy unit-tested), double-tap quick-assign (first empty slot, full toolbar replaces the last), and the tap path (select tile, tap slot / minus button) — which is also what the UI tests drive since XCUITest cannot synthesize drags between SwiftUI drop destinations; gesture-action slots open the gallery focused on their help panel (gestures are drawn, not tapped; item-based sheet presentation so first-open focus can't race state); versioned Codable persistence in UserDefaults (`ToolbarStore`: corrupt/missing/version-mismatched data falls back to default, unknown future action ids decode to empty slots) saved on every mutation and restored on launch — relaunch UI test proves exact restoration ("Toolbar persistence" mapped in traceability); `-UITestResetState` now also clears the stored toolbar. Deferred with 9.1: real per-action demo recordings. Toolbar repositioning beyond the existing left-handed mirror remains with the settings scope it shipped in (3.1)

- [x] 3.9 Pencil-only authoring policy (spec change 2026-07-21): remove the finger draw-mode fallback — fingers navigate exclusively (1-finger orbit / 2-finger pinch zoom / 2-finger pan / 3- and 4-finger tap undo/redo); UI tests migrate from finger draw-mode to the stroke-injection hook (spec: pencil-interaction "Finger strokes never author")
- [x] 3.10 FBX import pulled forward from 8.2: fileImporter accepts .fbx alongside .obj as Target or EditMesh (spec: scene-pipeline "Import formats")

## 4. Retopology tools

- [ ] 4.1 Build Quad / Build Triangle / Merge Pair / Path Distribute / Surface Cut (spec: retopology-tools)
- [ ] 4.2 Camera-as-manipulator tools: Patch Clone, Extend Boundary (all modes + grid fill + fans + auto-select), Draw Strip, Transform Vertices (spec: retopology-tools)
- [ ] 4.3 Pins (vertex + loop) and loop tags; Loop Info inspector (spec: retopology-tools)
- [ ] 4.4 Multi-axis + radial symmetry, apply-symmetry, re-symmetrize (spec: retopology-tools)
- [ ] 4.5 Auto Relax; batch commands (snap-all, relax-all, subdivide, triangulate, clears, subdivide+reproject) (spec: retopology-tools)
- [ ] 4.6 Subdivision preview (1–2 levels, reprojected) (spec: retopology-tools)

## 5. Weave — constraint-driven hybrid solve

- [ ] 5.1 Define/land solver API in engine (region + constraints → ghost mesh; progress, cancel, determinism contract); golden-file tests (spec: weave-solver)
- [ ] 5.2 Constraint plumbing from app: frozen patches, tagged loops, guide strokes, pins, density brush, symmetry (spec: weave-solver)
- [ ] 5.3 Prescribed-boundary interface guarantee: engine tests for exact boundary landing, interior-only singularities (spec: weave-solver)
- [ ] 5.4 Ghost accept/override flow + lasso-region solve UX; live regional re-solve on constraint edit (spec: weave-solver)
- [ ] 5.5 Implicit sizing from frozen interfaces; global density for full-mesh solve (spec: weave-solver)
- [ ] 5.6 Ambient assist mode (boundary next-patch ghosts, disableable) (spec: weave-solver)
- [ ] 5.7 Benchmark run vs AutoRemesher/Quadriflow/Instant Meshes; publish numbers backing the marketing claim (spec: weave-solver)

## 6. UV stage

- [ ] 6.1 Stage switcher + split view (swipe maximize, line re-split); UV-only project type (specs: document-model, uv-workflow)
- [ ] 6.2 Seam authoring 3D+2D, X-gesture unwrap, engine unwrap/relax solver with corner pinning (spec: uv-workflow)
- [ ] 6.3 On-surface UV manipulation (relax/move/pinch transform with live checker) + 2D island grammar, vertex mode, grid straighten, clone, stitch, partial symmetry (spec: uv-workflow)
- [ ] 6.4 Distortion + texel-density heatmaps (spec: uv-workflow)
- [ ] 6.5 Auto-seam ghost proposals respecting manual seams (spec: uv-workflow)
- [ ] 6.6 Metal-compute packing + manual aids (pack-to-region, grouping, overlap distribute, flip arrows) (spec: uv-workflow)
- [ ] 6.7 Symmetry-aware island stacking; multiple UV sets + UDIM tiles (spec: uv-workflow)

## 7. Baking stage

- [ ] 7.1 BK stage UI: side-by-side low/high viewports, draw-to-link components, X-to-bake, light-drag preview (spec: baking)
- [ ] 7.2 Brush-editable per-vertex cage (Relax/Tweak/Erase semantics) (spec: baking)
- [ ] 7.3 GPU bake core: Metal RT + intersector fallback, identical-output tests; normals + color first (spec: baking)
- [ ] 7.4 Full map set: AO, bent normals, curvature, thickness, position, ID (spec: baking)
- [ ] 7.5 Progressive live bake preview (spec: baking)
- [ ] 7.6 MikkTSpace golden-file verification vs Blender/Unity/Unreal; bake-mesh export (spec: baking)
- [ ] 7.7 Texture-to-texture rebake incl. UDIM (spec: baking)

## 8. Scene & pipeline

- [ ] 8.1 Outliner: show/solo/lock, groups, per-object stats; composition with lasso visibility (spec: scene-pipeline)
- [ ] 8.2 Importers: FBX, glTF/GLB, USD(z); image textures + 2D image targets (spec: scene-pipeline)
- [ ] 8.3 Exporters: STL, FBX, glTF/GLB, USD(z), multi-object, subdivide+reproject, UV sets/UDIM textures, Export folder (spec: scene-pipeline)
- [ ] 8.4 Live-link service (Bonjour + WebSocket): parity command set + camera streaming, off by default (spec: scene-pipeline)
- [ ] 8.5 Bidirectional delta edit sync + USD payloads (spec: scene-pipeline)
- [ ] 8.6 Python pip client + CLI; Blender add-on; Nomad round-trip preset; protocol docs (spec: scene-pipeline)

## 9. Onboarding, monetization, launch

- [ ] 9.1 Interactive first-run tutorial on bundled model (commission asset) (spec: pencil-interaction)
- [ ] 9.2 StoreKit 2 tiers (Free/Core/Studio, difference-priced upgrades, universal purchase), offline fail-open gating; saving never gated (spec: monetization)
- [ ] 9.3 Privacy audit: no telemetry, "Data Not Collected" label; network audit test (spec: monetization)
- [ ] 9.4 macOS shell (Catalyst or native SwiftUI) sharing document format + universal purchase
- [ ] 9.5 Localization scaffolding + first locales (JP/KR/zh-CN/pt-BR)
- [ ] 9.6 Device test plan + release gate: device-only suite (Metal RT, Pencil hover/haptics/squeeze, ProMotion, StoreKit) running on one RT-capable and one baseline device per release candidate, `.xcresult` archived with the tag; cross-environment determinism check (spec: quality-assurance)
- [ ] 9.7 App Store assets, TestFlight beta, `openspec archive add-cybertopology-app` after ship
