# add-auto-seam-proposals

Phase 6's 6.5: propose the remaining UV seams as a reviewable ghost, treating the artist's own seams as barriers chart growth will not cross — so it answers "given your cuts, where else" rather than re-deciding the layout. Only ever ADDS; accepting can never delete an authored seam. Known limitation stated up front: the chart-MERGE passes are not barrier-aware, so the union guarantees a correct result while the auto seams near a manual cut may be placed as if it were not there.
