# add-weave-guide-field-steering

Inject directional constraints (guide strokes + tagged-loop flow) into the engine cross-field so Auto-Retopo edge flow follows user hints (hard-pin, first-order steering)
