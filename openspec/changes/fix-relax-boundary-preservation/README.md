# fix-relax-boundary-preservation

Relax preserves the patch boundary (boundary vertices even out ALONG the boundary, not toward the interior) so it evens the interior quads instead of collapsing the patch into a star
