# Vendored: ufbx

Single-file FBX parser, vendored as a dedicated C target (task 3.10).

- Upstream: https://github.com/ufbx/ufbx
- Version: v0.23.0 (commit `fcc5d6ba444cfd3eb80677dba5e37e493941abe5`)
- License: MIT (dual MIT / public-domain-Unlicense; we elect MIT — permissive,
  passes the repo's permissive-only license gate). Full text in `LICENSE`.
- Files vendored: `ufbx.c`, `include/ufbx.h`, `LICENSE` — nothing else from
  the upstream repo.

Local modifications: none. To update, replace the two source files with the
same paths from a newer tag and update the version/commit above.
